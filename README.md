# Angular Paint

Standalone-приложение на Angular 22 и Angular Material с тёмной темой.

## Запуск

```bash
nvm use
npm install
npm start
```

Приложение будет доступно по адресу `http://localhost:4200/`.

## Сборка

```bash
npm run build
```
